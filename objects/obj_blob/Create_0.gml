/// @description Initialize state

if room == rm_level1 then set_location_state(LocationState.SpaceBeam)
if room == rm_level1_alt then set_location_state(LocationState.Ground)

power_up_state = PowerUpState.None
